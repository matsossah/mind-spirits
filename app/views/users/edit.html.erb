<div class="banner">
  <%= render "shared/navbar" %>
  <div class="container">
    <div class="col-xs-12 col-sm-6  col-sm-offset-3">
      <div class="login">
        <h2>Edit your profile</h2>
        <%= simple_form_for(@user) do |f| %>
          <%= f.error_notification %>

          <div class="form-inputs">
            <%= f.input :name, label: false, placeholder:'Your name'%>
            <%= f.input :phone_number,label: false, placeholder:'Your phone number'%>
            <%= f.input :address, input_html: { id: "user_input_autocomplete_address"}, label: false, placeholder:'Your address' %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Edit", id:'log' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

