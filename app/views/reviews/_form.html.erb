<%= simple_form_for([ current_user, event, @review ]) do |form| %>
  <%= form.input :rating, collection: [0, 1, 2, 3, 4, 5] %>
  <%= form.input :content %>
  <%= form.button :submit, class: "btn btn-xd btn-color-line" %>
<% end %>
